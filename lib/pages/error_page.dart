import 'package:flutter/material.dart';

class Error_Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body:
      Center(
        child:
        Container(child:
        Image.asset("image_assets/error.gif"),

        ),
      ),
    );
  }
}
