import "package:flutter/material.dart";

class Contactus extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Contact page",style: TextStyle(fontWeight: FontWeight.bold,fontSize: 28),
      ),
    );
  }
}

