import "package:flutter/material.dart";

class AboutUs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("About us page",style: TextStyle(fontWeight: FontWeight.bold,fontSize: 28),
      ),
    );
  }
}

