import "package:flutter/material.dart";


class HomePageContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Home Page content",style: TextStyle(fontWeight: FontWeight.bold,fontSize: 28),
      ),
    );
  }
}
