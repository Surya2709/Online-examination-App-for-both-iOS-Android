import "package:flutter/material.dart";

class NotificationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Time Table",style: TextStyle(fontWeight: FontWeight.bold,fontSize: 28),
      ),
    );
  }
}
