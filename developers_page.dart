import "package:flutter/material.dart";

class DevelopersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Developers page",style: TextStyle(fontWeight: FontWeight.bold,fontSize: 28),
      ),
    );
  }
}
